version: "3.9"

services:
  api:
    container_name: mini-api-container
    image: mini-api
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 80 --reload
    ports:
      - "80:80"
    volumes:
      - ./app:/code/app
  